#pragma once
#include "GameScene.h"

class GameScene_Parse : public GameScene
{
public:
    GameScene_Parse();
    virtual ~GameScene_Parse();

    virtual void Update();
    virtual void Output();

};
